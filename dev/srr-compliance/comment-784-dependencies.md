@maurolepore Thanks. Following up on the two points from the check results.

**1. Statistical standards.** Done: `srr` scaffolding and tags are now in the package ([kindling#37](https://github.com/joshuamarie/kindling/pull/37)), and the bot confirms compliance above the threshold ([73.4%](https://github.com/ropensci/software-review/issues/784#issuecomment-5329060105): general 56/68, ml 60/90). Note the reported percentage counts our 42 justified `@srrstatsNA` standards as "not complied with"; every standard we consider applicable is addressed, and no `@srrstatsTODO` tags remain.

**2. Package Dependencies note.** We checked both flagged Imports, and both are genuinely used:

- **`tibble`**: 18 namespaced calls in `R/`, e.g. `tibble::tibble()` in `R/mlp_kindling.R`, `R/rnn_kindling.R`, `R/gen-train-nn-parsnip.R`, `R/variable-imp.R`, and `tibble::as_tibble()` in `R/grid_depth.R` and `R/register-parsnip.R`. These build the prediction tibbles the parsnip engines must return.
- **`lifecycle`**: `importFrom(lifecycle, badge)` in `NAMESPACE`, a runtime call to `lifecycle::deprecate_soft()` in `R/act-fun.R` (the superseded `args()` helper), and five `` `r lifecycle::badge()` `` calls in roxygen documentation.

So we'd rather keep both in Imports. The `NA` entries look like blind spots of the code-tagging system rather than unused dependencies. The same table reports `NA` for `testthat` (used in all 31 test files), and for `recipes`, `rsample`, and `yardstick`, which appear in ~28 call sites across the tests and vignettes. The table's own caveat ("generated by an automated code-tagging system which may not be entirely accurate") seems to apply here.

**3. lintr.** Addressed as well, see [kindling#XX](https://github.com/joshuamarie/kindling/pull/XX). <!-- replace XX with the PR number once opened -->
