% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-load.R
\name{kindling-save-load}
\alias{kindling-save-load}
\alias{save_kindling}
\alias{load_kindling}
\title{Save and reload trained kindling models}
\usage{
save_kindling(object, path)

load_kindling(path)
}
\arguments{
\item{object}{A trained model of class \code{nn_fit}, \code{ffnn_fit}, or
\code{rnn_fit}.}

\item{path}{File path to write to / read from.}
}
\value{
\code{save_kindling()} returns \code{path} invisibly; \code{load_kindling()}
returns the restored model object.
}
\description{
\code{save_kindling()} writes a trained kindling model (an \code{nn_fit},
\code{ffnn_fit}, or \code{rnn_fit} object) to a file, and \code{load_kindling()}
restores it. Base \code{\link[=saveRDS]{saveRDS()}} is \strong{not} appropriate for these objects:
the underlying \code{torch::nn_module} holds external pointers into
LibTorch that do not survive ordinary R serialization. These functions
split the object instead -- the torch module is serialized with
\code{\link[torch:torch_serialize]{torch::torch_serialize()}} and everything else with R's serializer --
so the reloaded model is fully functional.

The reloaded model lives on the CPU (\verb{$device} is set to \code{"cpu"}),
regardless of the device it was trained on; move it with
\code{object$model$to(device = ...)} if needed. Reloaded fits can be used
directly with \code{\link[=predict]{predict()}}, and re-submitted to \code{\link[=train_nn]{train_nn()}} via its
\code{initial_model} argument to continue training.

Note for custom architectures: the \emph{instantiated} module is saved, so
prediction and continued training work after reload even for
\code{\link[=nn_arch]{nn_arch()}}-based models; only re-building the architecture from
scratch would require the original constructor objects.
}
\examples{
\donttest{
if (torch::torch_is_installed()) {
    fit = train_nn(
        x = as.matrix(iris[, 2:4]),
        y = iris$Sepal.Length,
        hidden_neurons = 8,
        epochs = 5
    )

    path = tempfile(fileext = ".rds")
    save_kindling(fit, path)

    fit2 = load_kindling(path)
    head(predict(fit2, newdata = as.matrix(iris[, 2:4])))

    # continue training from the reloaded model
    fit3 = train_nn(
        x = as.matrix(iris[, 2:4]),
        y = iris$Sepal.Length,
        initial_model = fit2,
        epochs = 5
    )

    unlink(path)
}
}

}
\seealso{
\code{\link[=train_nn]{train_nn()}} (\code{initial_model} argument) for continuing
training from a reloaded model.
}
